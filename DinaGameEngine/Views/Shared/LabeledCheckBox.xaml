<UserControl x:Class="DinaGameEngine.Views.Shared.LabeledCheckBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.InputBindings>
            <MouseBinding MouseAction="LeftClick" 
                      Command="{Binding ToggleCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        </Grid.InputBindings>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Column="0"
                   Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Style="{DynamicResource FieldLabelStyle}"
                   VerticalAlignment="Center"/>
        <CheckBox Grid.Column="1"
                  IsChecked="{Binding IsChecked, Mode=TwoWay, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  VerticalAlignment="Center"/>
    </Grid>
</UserControl>