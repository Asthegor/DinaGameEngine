<UserControl x:Class="DinaGameEngine.Views.UserControls.SceneCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DinaGameEngine.Views.UserControls"
             xmlns:ext="clr-namespace:DinaGameEngine.Extensions"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" VerticalAlignment="Center">
                <Run Text="{Binding SceneName, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}" Foreground="DimGray" FontWeight="SemiBold"/>
                <Run Text=" (" Foreground="Gray" FontSize="11"/>
                <Run Text="{Binding SceneKey, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}" FontFamily="Consolas" FontSize="11" Foreground="Gray"/>
                <Run Text=")" Foreground="Gray" FontSize="11"/>
            </TextBlock>
            <TextBlock Grid.Row="1" VerticalAlignment="Center">
                <Run Text="{ext:Translate Key=MainWindow_Scene_Components}" Foreground="DimGray" FontWeight="SemiBold"/>
                <Run Text="{Binding ComponentCount, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=OneWay}" FontFamily="Consolas" FontSize="11" Foreground="Gray"/>
            </TextBlock>
        </Grid>

        <local:ButtonBarView Grid.Column="1" HorizontalAlignment="Right"
                             DataContext="{Binding Buttons}" 
                             Margin="8,4,8,8"/>
    </Grid>
</UserControl>
