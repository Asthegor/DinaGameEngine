<UserControl x:Class="DinaGameEngine.Views.UserControls.ProjectHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DinaGameEngine.Views.UserControls"
             xmlns:panels="clr-namespace:DinaGameEngine.Views.UserControls.Panels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <local:ButtonBarView Grid.Row="1" 
                             DataContext="{Binding NavigationButtons}"/>

        <ScrollViewer Grid.Column="1" Background="Transparent">
            <ItemsControl ItemsSource="{Binding Scenes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <local:SceneCardView SceneName="{Binding Name}"
                                             SceneKey="{Binding Key}"
                                             ComponentCount="{Binding ComponentsCount}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>

</UserControl>
