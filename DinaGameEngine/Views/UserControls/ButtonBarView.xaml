<UserControl x:Class="DinaGameEngine.Views.UserControls.ButtonBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DinaGameEngine.Views.UserControls"
             xmlns:common="clr-namespace:DinaGameEngine.Common;assembly=DinaGameEngine.Common"
             xmlns:converters="clr-namespace:DinaGameEngine.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
    </UserControl.Resources>
    <ItemsControl ItemsSource="{Binding Buttons}"
                  HorizontalAlignment="{Binding HorizontalAlignment}"
                  VerticalAlignment="{Binding VerticalAlignment}">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <UniformGrid Rows="1"/>
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemContainerStyle>
            <Style TargetType="ContentPresenter">
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
            </Style>
        </ItemsControl.ItemContainerStyle>
        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <Button Command="{Binding Command}" Margin="4" Padding="16,8"
                        Height="{Binding DataContext.ButtonHeight, RelativeSource={RelativeSource AncestorType=ItemsControl}}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource GhostFooterButton}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Role}" Value="{x:Static common:ButtonRole.Primary}">
                                    <Setter Property="Background" Value="DarkViolet"/>
                                    <Setter Property="BorderBrush" Value="DarkViolet"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="Tag" Value="#532E80"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Role}" Value="{x:Static common:ButtonRole.Secondary}">
                                    <Setter Property="BorderBrush" Value="OrangeRed"/>
                                    <Setter Property="Foreground" Value="OrangeRed"/>
                                    <Setter Property="Tag" Value="#FFECB3"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Role}" Value="{x:Static common:ButtonRole.Neutral}">
                                    <Setter Property="BorderBrush" Value="DarkViolet"/>
                                    <Setter Property="Foreground" Value="DarkViolet"/>
                                    <Setter Property="Tag" Value="#F3E5F5"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding IsEnabled, RelativeSource={RelativeSource Self}}" Value="False">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderBrush" Value="Gray"/>
                                    <Setter Property="Foreground" Value="Gray"/>
                                    <Setter Property="Tag" Value="#F5F5F5"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Icon}" 
                                   FontSize="14"
                                   Margin="0,0,8,0" VerticalAlignment="Center"
                                   Visibility="{Binding Icon, Converter={StaticResource NullToVisibilityConverter}}"/>
                        <TextBlock Text="{Binding Label}" 
                                   VerticalAlignment="Center" 
                                   FontSize="14" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
