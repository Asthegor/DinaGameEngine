<UserControl x:Class="DinaGameEngine.Views.Startup.MarkerItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DinaGameEngine.Views.Startup"
             xmlns:ext="clr-namespace:DinaGameEngine.Extensions"
             xmlns:conv="clr-namespace:DinaGameEngine.Converters"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="600">
    <UserControl.Resources>
        <conv:LocalizationConverter x:Key="LocalizationConverter"/>

        <Style x:Key="MarkerTextBoxStyle" TargetType="TextBox">
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="DarkViolet"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="OrangeRed"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding IsEmpty}" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="Red"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="0,4">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="6*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" VerticalAlignment="Center">
            <Run Text="{Binding Label, Converter={StaticResource LocalizationConverter}, Mode=OneWay}" 
                 Foreground="DimGray" 
                 FontWeight="SemiBold"/>
            
            <Run Text=" (" Foreground="Gray" FontSize="11"/>
            
            <Run Text="{Binding Key, Mode=OneWay}" 
                 FontFamily="Consolas" 
                 FontSize="11" 
                 Foreground="Gray"/>
            
            <Run Text=")" Foreground="Gray" FontSize="11"/>
        </TextBlock>

        <Button Grid.Column="1"
                Style="{DynamicResource GhostFooterButton}"
                Command="{Binding ResetMarkerCommand}"
                Visibility="{Binding IsNotDefaultValue, Converter={StaticResource BoolToVisibility}}"
                Width="30" Height="30" FontSize="12"
                FontFamily="Segoe Fluent Icons" 
                Content="{Binding RefreshIcon, Mode=OneWay}"
                Tag="#F3E5F5" BorderBrush="DarkViolet" Foreground="DarkViolet"/>

        <TextBox Grid.Column="2"
                 Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                 Style="{StaticResource MarkerTextBoxStyle}"/>
    </Grid>
    
</UserControl>
