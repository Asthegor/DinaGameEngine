<UserControl x:Class="DinaGameEngine.Views.Project.Editors.SceneEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DinaGameEngine.Views.Project.Editors"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="4"/>
            <ColumnDefinition Width="280"/>
        </Grid.ColumnDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Grid.Column="1"
                        x:Name="AddComponentButton"
                        Click="AddComponentButton_Click"
                        Content="{Binding AddIcon}"
                        FontFamily="Segoe Fluent Icons"
                        FontSize="20">
                    <Button.ContextMenu>
                        <ContextMenu ItemsSource="{Binding AvailableGenerators}">
                            <ContextMenu.ItemContainerStyle>
                                <Style TargetType="MenuItem">
                                    <Setter Property="Header" Value="{Binding ComponentType}"/>
                                </Style>
                            </ContextMenu.ItemContainerStyle>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>
            </Grid>

            <ScrollViewer Grid.Row="1">
                <ItemsControl ItemsSource="{Binding FilteredComponents}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <Run Text="{Binding Key, Mode=OneWay}" Foreground="DimGray" FontWeight="SemiBold"/>
                                <Run Text=" (" Foreground="Gray" FontSize="11"/>
                                <Run Text="{Binding Type, Mode=OneWay}" FontFamily="Consolas" FontSize="11" Foreground="Gray"/>
                                <Run Text=")" Foreground="Gray" FontSize="11"/>
                            </TextBlock>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <GridSplitter Grid.Column="1"
                      x:Name="LeftSplitter"
                      Background="#E0E0E0"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"/>

        <Border Grid.Column="2"
                Background="Black"/>

        <GridSplitter Grid.Column="3"
                      x:Name="RightSplitter"
                      Background="#E0E0E0"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch"/>

        <Border Grid.Column="4"
                Background="#F9F9F9"/>

    </Grid>
</UserControl>
