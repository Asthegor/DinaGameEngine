<UserControl x:Class="DinaGameEngine.Views.Project.Components.ColorCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:shared="clr-namespace:DinaGameEngine.Views.Shared"
             xmlns:converters="clr-namespace:DinaGameEngine.Converters"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Themes/CardItem.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:ColorToBrushConverter x:Key="ColorToBrushConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border Style="{StaticResource CardItemBorderStyle}" CornerRadius="10" Padding="5" Margin="0,2,0,2">
        <Grid Background="Transparent">
            <Grid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding OpenCommand}"/>
                <MouseBinding MouseAction="LeftClick" Command="{Binding SelectCommand}"/>
            </Grid.InputBindings>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="48"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0"
                    Width="32" Height="32"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    BorderBrush="Black"
                    BorderThickness="1">
                <Rectangle Fill="{Binding PreviewColor, Converter={StaticResource ColorToBrushConverter}}"/>
            </Border>
            <TextBlock Grid.Column="1"
                       Text="{Binding Key}"
                       VerticalAlignment="Center"
                       Foreground="DimGray"
                       FontWeight="SemiBold"/>
            <shared:ButtonBarView Grid.Column="2"
                                  DataContext="{Binding NavigationButtons}"
                                  Margin="8,4,8,8"/>
        </Grid>
    </Border>
</UserControl>