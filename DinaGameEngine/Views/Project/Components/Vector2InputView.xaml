<UserControl x:Class="DinaGameEngine.Views.Project.Components.Vector2InputView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DinaGameEngine.Views.Project.Components"
             xmlns:ext="clr-namespace:DinaGameEngine.Extensions"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0"
                   Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Style="{DynamicResource FieldLabelStyle}"
                   Margin="0,0,0,4"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0"
                     Text="{Binding ValueX, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     Style="{DynamicResource ModernTextBoxStyle}"
                     Margin="0,0,0,8"/>
            <TextBox Grid.Column="1"
                     Text="{Binding ValueY, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     Style="{DynamicResource ModernTextBoxStyle}"
                     Margin="0,0,0,8"/>
            <Button Grid.Column="2"
                    Margin="4,0,4,0"
                    Content="{Binding ResetIcon, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    FontFamily="Segoe Fluent Icons"
                    FontSize="14"
                    Command="{Binding ResetCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Style="{DynamicResource GhostFooterButton}"
                    ToolTip="{ext:Translate Key=Component_Reset_Tooltip}"
                    Width="28" Height="28"
                    BorderBrush="OrangeRed"
                    Foreground="OrangeRed"
                    Tag="#FFECB3"/>
        </Grid>
    </Grid>
</UserControl>
